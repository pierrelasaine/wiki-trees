image: python:3.11.0
lint:
 stage: test
 script:
 - pip install yapf==0.32.0
 - yapf --recursive . --style google --diff
 only:
 - pushes

unittest:
 stage: test
 dependencies:
 - lint
 script:
 - pip install -r requirements.txt
 - pytest
 only:
 - pushes